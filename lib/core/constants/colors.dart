import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF253690);
  static const Color secondary = Color(0xFFE8B313);
  static const Color white = Color(0xFFFFFFFF);
  static const Color grey = Color(0xFF555555);
  static const Color black = Color(0xFF000000);
  
  // Navigation colors
  static const Color navSelected = primary;
  static const Color navUnselected = grey;
  
  // Button colors
  static const Color buttonBackground = primary;
  static const Color buttonText = white;
  
  // Shadow colors
  static const Color shadowColor = Color(0x1A000000); // 10% black

  // Text colors
  //static const Color textColor = Color(0xFF333333);
}